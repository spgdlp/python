pipeline {
  agent any
  stages {
    stage('python version') {
      steps {
        bat 'python --version'
      }
    }
    stage('java version') {
      steps {
        bat 'java --version'
      }
    }
  }
}
