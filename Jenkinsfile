pipeline {
  agent any
  stages {
    stage('java version') {
      steps {
        bat 'java --version'
      }
    }
    stage('python version') {
      steps {
        bat 'python --version'
      }
    }
  }
}
